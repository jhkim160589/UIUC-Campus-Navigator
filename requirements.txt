# --- core ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
psycopg[binary]==3.2.3        # Postgres driver. Raw SQL on purpose - no ORM.
python-dotenv==1.0.1
httpx==0.28.1                 # UIUC API fetching

# --- dev ---
pytest==8.3.4
pytest-asyncio==0.25.0
